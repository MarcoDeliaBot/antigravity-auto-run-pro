# TODO

- [ ] Pubblicare l'estensione con il comando `npx vsce publish --pre-release` per far apparire il pulsante nativo "Switch to Pre-Release Version" nel Marketplace di VS Code. (Richiede l'inserimento del Personal Access Token)
